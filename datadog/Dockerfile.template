FROM balenalib/%%BALENA_MACHINE_NAME%%:jessie-build

RUN apt-get update && apt-get install -yq sysstat \
    python \
    stress \
    wget \
    python-dev \
    python-pip git && \
    apt-get clean && rm -rf /var/lib/apt/lists/*

COPY ./start.sh /usr/src/app/start.sh

CMD ["/bin/sh","/usr/src/app/start.sh"]